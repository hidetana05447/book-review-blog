# 日常の視点を広げる書評ブログ

## サイト概要
- サイト名：日常の視点を広げる書評ブログ
- サブテキスト：小説から哲学まで、読んだ本から得た気づきを日常に結びつけて綴っていきます。
- 技術：素のHTML/CSS（ビルドツールは使わない）

## ページ構成
- `index.html`：TOPページ（サイト名・サブテキスト・記事一覧を3列グリッドで表示）。カードは**公開日が新しい順**（左上から）に並べる。各カードに公開日（`.review-date`）を表示する
- `reviews/`：書評ページ（1記事1ファイル）
- `images/thumbnails/`：記事ごとのサムネイル画像
  - `〇〇.svg`：ページ内表示用（記事本文中のヘッダー画像・TOPページのカード）
  - `〇〇-og.png`：OGP/Twitterカード用（1200×630のPNG。SVGのままだとXでのカード表示が不安定なため、`qlmanage -t -s 1200`でラスタライズ→`sips -c 630 1200`で1200×630にセンタークロップして生成）
  - 書影は著作権上の理由で使わず、オリジナルのテキストベース画像を作る
- `style.css`：全ページ共通のデザイン
- `sitemap.xml`：検索エンジン向けのページ一覧。**新しい記事を追加するたびに、このファイルにもURLを追記すること**
- `robots.txt`：`sitemap.xml`の場所をクローラーに知らせる設定（通常、追加の変更は不要）

## デザインの方向性
- 参考：sairu.co.jp/blog（フラットな背景・テキスト中心・写真や書影を使わない）
- 背景は白、本文はゴシック体（明朝体は使わない）
- サムネイルは薄いクリーム色の背景＋濃い枠線の「カード」に、書名・フックになる一言・ジャンルタグを配置

## 記事フォーマット
- 対象書籍（1冊、または複数冊。書名＋著者）
- ジャンルタグ
- 本文中に小見出しを入れて区切る
- 前半で軽く「あらすじ・この本について」に触れる（この際、著者名を`.review-meta`行だけでなく、本文の地の文にも自然に盛り込む。SEO・構造化データの`author`情報と対応させるため）
- 気づき→日常への当てはめ、という流れを地の文に自然に組み込む
- 小見出し（セクション）は最小4個〜最大8個程度を目安にする。Notionのメモの情報量に応じて調整してよい。8個を超えるボリュームになりそうな場合は、無理に1本に収めず「嫌われる勇気」のように前編・後編に分割する
- 「なぜこの本を手に取ったか」は、特別なエピソードがあるときだけ触れる（無理に書かない）
- 末尾に小さく注記：「※本記事はNotionに書き溜めた読書メモをもとに、AIの編集サポートを受けて構成しています。」

### 新しい記事を追加するときのチェックリスト（SEO関連）
1. `images/thumbnails/`に、その記事用のSVG（本文表示用）と`-og.png`（OGP用、上記コマンドで生成）を作る
2. `<head>`に以下を設定する：`<title>`／`<meta name="description">`（「広がった視点」の一言を使う）／`og:type`（article）・`og:site_name`・`og:title`・`og:description`・`og:image`（絶対URL）・`og:url`・`article:published_time`／`twitter:card`（summary_large_image）・`twitter:title`・`twitter:description`・`twitter:image`
   - **注意**：`content="..."`属性値の中で本文タイトルに`"`（ストレートクォート）が含まれる場合は、そのままだと属性が壊れるので`&quot;`にエスケープする
3. `<img>`タグ（TOPページのカード・記事ヘッダー画像）には、必ず内容の分かる`alt`属性を入れる（空にしない）
4. 本文中の`.review-meta`行に「公開日：YYYY年M月D日」を追記する
5. `index.html`の一覧にカードを追加する（サムネイル・ジャンル・タイトル・一言・alt属性・公開日）。**公開日が新しい順になるよう、正しい位置に挿入する**（末尾に追加するだけにしない）
6. `sitemap.xml`にそのページのURLを追記する
7. `<head>`内に`schema.org`の構造化データ（JSON-LD）を追加する
   - **単体書籍のレビュー**：`Review`型。`itemReviewed`に書籍名・著者、`author`/`publisher`はサイト名、`reviewBody`は要約文。星評価などの`reviewRating`は、実際に評価点を設けていないので入れない
   - **複数冊にまたがるテーマ記事**：`Review`は対象が1冊しか想定していないため使わない。代わりに`Article`型にして、対象の書籍群は`about`配列（各要素に`@type: Book`・書名・著者）で紐づける
8. `index.html`内の`Blog`構造化データ（JSON-LD）の`blogPost`配列にも、新しい記事を`headline`/`url`/`datePublished`付きで追加する（こちらも公開日順を維持）

## コンテンツ制作フロー
- Notionの「読書フォルダ」データベースが原本。このリポジトリには生メモを複製しない
- 感想の中の「ガネーシャ」（AIキャラ）を、キャラクターとして・AIの発言として記事に出すことはしない。ただし、そのやり取りの中で整理された概念・視点・解釈は、本人の言葉として地の文に落とし込んで使ってよい（例：嫌われる勇気の「原因論／目的論」の整理）
- 個人情報（勤務先名などの固有名詞、体調・通院など機微な内容、詳細すぎる経歴）は、下書きの時点でAI側から抽象化・伏せ字化を提案する。本人の指摘を待たない
- Notionのメモが薄い（テンプレートのまま等）場合は、無理に一本の記事に仕立てず、①分量なりに短く書く／②関連する他のノートを横断して材料を探す／③別の本に差し替える、を本人と相談して決める
- 下書きは会話の中で確認を得てから確定する

## SEO・アクセス計測
- Googleアナリティクス（GA4）を導入済み。測定ID：`G-TMXRHLCBLW`（全ページの`<head>`にgtag.jsを設置済み）
- Google Search Consoleを設定済み（URLプレフィックスプロパティ、GA連携で所有権確認）。サイトマップ送信済み
- GitHubの「Traffic（Insights > Traffic）」は、公開サイトの閲覧数ではなく「リポジトリ（コード）が見られた回数」でしかないので、実際のPV/UUの確認には使えない。必ずGAの方を見る
- 新しい記事を追加したら、`sitemap.xml`の更新を忘れないこと（上記チェックリスト参照）

## 書籍ラインナップ

### 公開済み
- ヒカルの碁（原作：ほったゆみ／作画：小畑健）※通常の読書メモではなく、AIとの協働論をまとめた思索ノートが原本。漫画作品のため、新規ジャンルタグ「漫画」を追加
- 豊臣秀長（堺屋太一）※関連ノートに実際の職場でのキャリア交渉・役職戦略の詳細な内容があったため、史実と「支える役割への共感」という文学的な核だけを残し、具体的な交渉内容・役職名・組織構造の話は大きく抽象化して構成
- カウンセリングとは何か 変化するということ（東畑開人）※Notionの書評メモではなく、本と自身の実体験を結びつけた内省ノート2本を原本として構成。組織名など固有人物・組織を特定できる情報は抽象化済み
- 傲慢と善良（辻村深月）
- 43歳頂点論（角幡唯介）
- 暇と退屈の倫理学（國分功一郎）
- 嫌われる勇気（岸見一郎・古賀史健）※前編・後編の2記事構成、相互リンクあり
- コンビニ人間（村田沙耶香）※Notion本体はメモ薄めだったが、関連ノート（ガネーシャとの壁打ち）の視点も本人の言葉として使用
- 人生の壁／スピノザの診療室（養老孟司×夏川草介）※複数冊テーマ記事。構造化データは`Review`ではなく`Article`＋`about`配列を使用
- アルジャーノンに花束を（ダニエル・キイス）※Cursorでの下書きテストを経て、こちらで文字化け修正・仕上げ
- 正欲／何者／イン・ザ・メガチャーチ（朝井リョウ）※単体レビュー3本＋テーマまとめ記事1本の計4本構成。まとめ記事は`Article`＋`about`配列、単体は`Review`。各単体レビューとまとめ記事は相互リンクあり

### 第一弾・未着手
- 単体レビュー：仕事なんか生きがいにするな

### 劣後（後日追加）
むらさきのスカートの女／人生の経営戦略／沈黙の春

## 運用ルール
- マージ・GitHub Pagesでの公開は、必ずヒデタナさん自身がGitHub上で行う。凛はブランチのpushとPR作成用リンクの共有までで作業を止める。技術的に直接pushできる状態であっても（`main`にブランチ保護がかかっていない場合など）、それを理由に代行しない
- 記事の本文確認は、見出し・要点だけの構成案ではなく、本文全文をチャットに直接貼った状態で行う。ブラウザプレビューは補助的な見た目確認であり、本文確認の代わりにはしない
- 記事本文中に挿絵・概念図を入れたい場合は`.claude/skills/book-review-illustration/`を使う（2026-08-19導入）。サムネイル・OGP画像とは別物。このサイト専用の画風・配色（半写実的な水彩画調、クリーム×焦げ茶×琥珀）が確立済みなので、別セッションから依頼する場合もこのSkillを呼び出せば同じトンマナで作れる
